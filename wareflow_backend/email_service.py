def send_otp_email(to_email: str, otp: str, purpose: str) -> bool:
    print(f"[EmailJS Bypass] send_otp_email() called. to_email={to_email}, otp={otp}, purpose={purpose}", flush=True)
    return True
